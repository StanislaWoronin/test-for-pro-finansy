import { UserModel } from '../../../modules/auth/repositories/models/user.model';
import { SessionModel } from '../../../modules/auth/repositories/models/session.model';

export const models = [SessionModel, UserModel];
